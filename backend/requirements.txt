fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Intentionally unpinned: YouTube changes often and only the newest yt-dlp
# keeps working. Rebuild the Render image (clear build cache) to refresh it.
yt-dlp
